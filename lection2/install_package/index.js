const annaMatchPackage = require('anna_math_package');

const addResult = annaMatchPackage.add(3, 9);
const subResult = annaMatchPackage.subtract(9, 3);

console.log(addResult);
console.log(subResult);